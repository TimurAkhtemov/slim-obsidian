# SLIM — agent brief

**Read [`CLAUDE.md`](CLAUDE.md).** It is the whole operating brief: what SLIM is, what was
deliberately cut and must not be rebuilt, the traps that cost real debugging time (stale
server on port 7546, Ollama's thinking default, `num_ctx` per model), the rules that still
bind. What is next is in `BACKLOG.md`.

This file used to be a second copy of that brief and drifted out of date. Two documents
saying the same thing means one of them is wrong; keeping a pointer is cheaper than keeping
them in sync.

The decision log (why things are the way they are) and the measured experiments live in the
owner's vault, outside this repository — `Notes/personal-intelligence/slim-docs/`.
